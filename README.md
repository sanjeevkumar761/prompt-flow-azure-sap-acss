Create venv with "python -m venv sap-venv"  
source ./sap-venv/Scripts/activate  
Save .env-sample file as .env and update values of environment variables  
pip install -r requirements.txt  
python server.py  

